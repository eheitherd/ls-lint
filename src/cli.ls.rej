diff a/src/cli.ls b/src/cli.ls	(rejected hunks)
@@ -5,7 +5,9 @@ require! {
   commander: program
   '../package.json': my-package
   './load-config'
+  './default-config': {read-default-config}
   './lint-files'
+  './reporters/report-utils': {print}
   './reporters/report-total'
   './reporters/report-error'
 }
@@ -20,12 +22,21 @@ module.exports = (argv) ->
     .description 'lint livescript source files'
     .usage '[options]... [file]...'
     .option '-c, --config <file>' 'use specified configuration file'
+    .option '--print-config' 'print the configuration'
     .parse process.argv
 
-  # Prints help and exits when no arguments is given.
-  if program.args.length is 0
-    program.help!
-    process.exit 1
+  # Print config
+  if program.print-config
+    if program.config
+      try
+        config = load-config program.config
+      catch e
+        report-error e
+        process.exit 1
+      ...
+    else
+      print read-default-config!
+    process.exit 0
 
   # Loads configuration from files
   try
@@ -34,6 +45,11 @@ module.exports = (argv) ->
     report-error e
     process.exit 1
 
+  # Prints help and exits when no arguments is given.
+  if program.args.length is 0
+    program.help!
+    process.exit 1
+
   # Lints required files.
   program.args
   |> lint-files _, {config}
